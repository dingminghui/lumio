export const LONG_IMAGE_SKILL_ID = "long-image";

/** 长图节点默认画布尺寸，便于完整预览长图内容 */
export const LONG_IMAGE_DEFAULT_WIDTH = 400;
export const LONG_IMAGE_DEFAULT_HEIGHT = 800;

/** 长图预览/导出内容区宽度（节点宽度减去左右内边距） */
export const LONG_IMAGE_CONTENT_WIDTH = 376;

/** 节点头部 + 底部状态栏 + 内容区上下内边距 */
export const LONG_IMAGE_NODE_CHROME_HEIGHT = 80;

/** 长图节点可拖拽的最大高度（按内容自动撑开，上限放宽） */
export const LONG_IMAGE_MAX_HEIGHT = 4800;

/** 空状态节点最小高度 */
export const LONG_IMAGE_EMPTY_MIN_HEIGHT = 320;
