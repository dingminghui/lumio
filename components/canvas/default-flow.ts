import type { Edge, Node } from "@xyflow/react";

export const defaultNodes: Node[] = [
  {
    id: "welcome",
    type: "input",
    data: { label: "Lumio Canvas" },
    position: { x: 120, y: 120 },
  },
];

export const defaultEdges: Edge[] = [];
